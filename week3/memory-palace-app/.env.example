# Copy to .env.local and fill in. Do not commit .env.local.

# Required for AI (associations + later vision)
OPENAI_API_KEY=sk-...

# Optional: cap tokens per request so competition usage stays predictable (default 512)
AI_MAX_TOKENS=512
